# raspberry-pi-translate
This project is an audio recording and playback system using a Raspberry Pi. It captures audio when a button is pressed, encodes it as a WAV file, and sends it to a remote API. It also supports downloading and playing audio files from a URL, using GPIO for button input and LED feedback, along with WebSocket communication for real-time interaction.
